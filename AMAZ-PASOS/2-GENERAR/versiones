0-main.py  -> abro un fichero - leo fichero - creo un nuevo fichero 
              que guardo en el nombre que he puesto en el fichero
./0-main.py laberinto.txt 


1-main.py  -> abro fichero - abro config.txt - creo un fichero con output.txt (este fichero output se actualiza con el nombre en config.txt)
      |        - escribo sobre el y cierro -> cuando ejecutes, saldra un fichero nuevo con los datos que le envio desde generador
      L-> ficheros: 1-main.py    -o-  (1-main-carpeta:) a-maze-ing.py + config_params.py + maze.py
Lo puedes lanzar de las dos formas.
./1-main.py config.txt 
./a-maze-ing.py config.txt 


2-main.py  -> a partir de aqui tengo ya una estructura de ficheros y archivos que voy a manterner. Indicare donde estan los cambios.
              en maze.py genero un primer laberinto, con las coordenadas que debo tener en cuenta oara montar bien el laberinto real:
                  - muros del contorno    - entrada y salida      - el 42 central
./a-maze-ing.py config.txt  --> genera maze.txt


3-main.py  -> incorporo seed() en config y maze -> probar el seed
              incorporo los numeros posibles para los muros y las esquinas en maze
              genero solo eso y lo anterior
./a-maze-ing.py config.txt  --> genera maze.txt


4-main.py -> configurar el resto del laberinto    --- Mucha ayuda IA
	     dudas: - ¿y si lo genero y me crea islas?
	     		- ¿y si no tienen ningun camino de salida?
	     notas: hay que darle una estructura a generacion de laberinto mas adelante, porque se ve largo
	4.1-main.py -> Extructura de Maze nueva
	4.2-main.py -> Funcion para el resto de las casillas  - generate_rest()
	4.3-main.py -> Dibujar, usando el drawing del principio
--- ESTA FORMA DE GENERAR EL RESTO NO ES VALIDA. se ve en el DRAWING, que el plano creado no cumple especicficaciones.

5-main.py  -> version final, con los errores de 4.3-main.py solucionados --> los errores estaban en el muro no en el rest()
	-- esta no es una buena forma de construir laberintos, porque nunca tienen salida
	
	
EXPLICACION EXTENDIDA

	generate_rest()   --- Mucha ayuda IA
	Esta funcion va a leer las casillas de maze. Va a empezar en la casilla : maze(1,1)-; y va a leer las casillas vecinas : maze(0,1) maze(1,0) maze(2,1) y maze(1,2).
    Dentro de estas casillas he guardado un numero hexadecimal (en las funciiones generate anteriores). Ese numero hexadecimal lo voy a pasar a binario. 
	Para la maze(0,1)-arriba- me voy a fijar en el bit del sur-abajo: bit de la posicion 2. 
	Para la maze(1,0)-izquierda- me voy a fijar en el bit del derecha-este-: bit de la posicion 1.  
	Para la maze(2,1)-abajo- me voy a fijar en el bit del arriba-norte-: bit de la posicion 0.   
	Para la maze(1,2)-derecha- me voy a fijar en el bit del izquierda-oeste-: bit de la posicion 3.  
	Con esos bits, voy a crear una mascara. Voy a ver los valores hexadecimales del 0-F que cumplen esta mascara 
	-nota: si la casilla a la que estoy preguntando [ maze(0,1) maze(1,0) maze(2,1) y maze(1,2) ] esta vacia no la tengas en cuenta para la mascara. 
	Con esa mascara, tengo que construir una sentencia parecida a esta: filtrados_13 = [num for num in hex_numeros if (num & 0b1010) == 0]
	
	     
	     FILTRADOS POR NUM HEX:
	     # Filtrar los numeros hexadecimales que tienen bits 0 y 1 a 0 (múltiplos de 4)
		filtrados = [num for num in hex_numeros if (num & 0b11) == 0]

		# Filtrar los numeros hexadecimales que tienen bits 0 y 2 a 0
		# bits 0 y 2 -> máscara binaria 0b101 = 5
		filtrados = [num for num in hex_numeros if (num & 0b101) == 0]

		# Filtrar los numeros hexadecimales que tienen bits 0 y 3 a 0
		# bits 0 y 3 -> máscara 0b1001 = 9
		filtrados = [num for num in hex_numeros if (num & 0b1001) == 0]

		hex_numeros = range(16)  # 0..15

		# Bits 1 y 2 a 0
		filtrados_12 = [num for num in hex_numeros if (num & 0b110) == 0]
		print("Bits 1 y 2 = 0:", [hex(n) for n in filtrados_12])  # ['0x0', '0x1', '0x8', '0x9']

		# Bits 1 y 3 a 0
		filtrados_13 = [num for num in hex_numeros if (num & 0b1010) == 0]
		print("Bits 1 y 3 = 0:", [hex(n) for n in filtrados_13])  # ['0x0', '0x1', '0x4', '0x5']

		# Bits 2 y 3 a 0
		filtrados_23 = [num for num in hex_numeros if (num & 0b1100) == 0]
		print("Bits 2 y 3 = 0:", [hex(n) for n in filtrados_23])  # ['0x0', '0x1', '0x2', '0x3']
		
hex1 = 0x5   # binario 0101 -> bit2 (pos2) = 1? No, bit2 es el tercer bit (valor 4). Si quieres el bit de la posición 1 de hex1 (valor 2), sería 0.
hex2 = 0x3   # binario 0011 -> bit0 = 1

# Usar el LSB de cada uno
mascara = ((hex1 & 1) << 2) | ((hex2 & 1) << 0)
print(bin(mascara))  # 0b100 (4) si hex1 LSB=1, hex2 LSB=1 -> 0b101 (5)



# pasar de un string hexadecimal a un binario:
hex_str = "A3F"
binario = bin(int(hex_str, 16))[2:]  # elimina el prefijo '0b'
print(binario)  # '101000111111'

hex_str = "A3F"
componentes = [format(int(d, 16), '04b') for d in hex_str]
print(componentes)  # ['1010', '0011', '1111']


		ALGORITMOS
		Prim, Kruskal, backtracking recursivo
