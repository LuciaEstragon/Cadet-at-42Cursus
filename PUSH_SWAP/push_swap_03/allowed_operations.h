#ifndef ALLOWED_OPERATIONS_H
# define ALLOWED_OPERATIONS_H

#include "libft_redo.h"

void swap(t_stack **stack);
void rotate(t_stack **stack);
void reverse(t_stack **stack);
void push(t_stack **orig, t_stack **dest);

#endif